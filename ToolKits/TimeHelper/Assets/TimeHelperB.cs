using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimeHelperB : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log("TimeHelperB Start!");
	}
	
	// Update is called once per frame
	void Update () {
		Debug.Log("TimeHelperB Update!");
	}
}
